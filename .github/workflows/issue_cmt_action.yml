name: issue_cmt_action

on: issue_comment

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: test msg
        run: echo this is a test message on issues cmt